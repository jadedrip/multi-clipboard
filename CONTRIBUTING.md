# 贡献指南

感谢您对本项目的关注！我们欢迎任何形式的贡献：报告问题、改进文档、修复缺陷、新增功能。

## 目录

- [开始之前](#开始之前)
- [报告问题](#报告问题)
- [提交代码](#提交代码)
- [代码规范](#代码规范)
- [测试](#测试)
- [提交信息规范](#提交信息规范)

## 开始之前

- 本项目使用 **中文** 作为沟通与注释语言
- 代码基于 **C++20** 与 **Qt6**，构建系统为 **xmake**
- 开发前请先阅读 [docs/design.md](docs/design.md) 与 [docs/architecture.md](docs/architecture.md)，了解模块划分与约定

## 报告问题

提交 Issue 时请包含：

- 运行环境（操作系统、Qt 版本、构建方式）
- 复现步骤
- 预期行为与实际行为
- 相关日志（位于 `%APPDATA%\MultiClipboard\logs`，Windows）

## 提交代码

1. 从 `main` 分支新建功能分支：`git checkout -b feature/your-feature`
2. 在分支上完成修改，并补充 / 更新对应的单元测试
3. 本地验证构建与测试通过（见 [README.md](README.md#运行测试)）
4. 提交 Pull Request，描述改动内容、动机与验证结果

## 代码规范

- 注释使用中文，采用 Javadoc 风格（`/** ... */`），便于工具处理
- **每个类独立定义在单独文件中**，文件名与类名一致
- 保持模块分层：核心业务在 `src/core/`，平台相关在 `src/platform/`，UI 在 `src/ui/`，通用工具在 `src/utils/`
- **UI 与业务分离**：UI 只负责界面与交互，通过 Qt 信号 / 槽与核心层通信，业务逻辑写在核心层
- 代码注重可读性：避免复杂语法与缩写；重复逻辑提取为函数或类
- 使用 Qt6 API，避免使用已废弃接口

## 测试

- 单元测试使用 Qt Test 框架，位于 `test/` 目录
- 新增或修改核心逻辑时，请同步编写或更新测试用例
- 本地运行测试：

  ```powershell
  # Windows：先配置 Qt 工具目录到 PATH
  xmake f -y -m release
  xmake
  xmake r test_content_parser
  ```

- 提交前确保测试全部通过（退出码为 0）

## 提交信息规范

提交信息使用中文，格式：

```
类型(模块): 简要描述

详细说明（可选）
```

类型说明：

| 类型 | 用途 |
| :--- | :--- |
| `feat` | 新增功能 |
| `fix` | 修复缺陷 |
| `docs` | 文档变更 |
| `refactor` | 重构（不改变行为） |
| `test` | 测试相关 |
| `build` | 构建 / 依赖变更 |
| `chore` | 其他杂项 |

示例：

```
fix(core): 修复 Excel 格式解析时空行列处理错误
```
